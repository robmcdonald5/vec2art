# Frontend-specific overrides and deployment artifacts

# Deployment platforms (not covered by root gitignore)
.output
.vercel
.netlify
.wrangler

# WASM duplicate directories (generated at build time by scripts/copy-wasm.js)
# Only src/lib/wasm/ main files are committed - these are regenerated for builds
src/lib/wasm/pkg/
src/lib/wasm/pkg-simd/
static/wasm/

# Environment files (frontend-specific)
.env
.env.*
!.env.example
!.env.test
!.env.ci

# Frontend-specific temporary files
.turbo

# Testing artifacts
test-results/
playwright-report/
tests/e2e/fixtures/images/
tests/e2e/fixtures/outputs/

*storybook.log
storybook-static
